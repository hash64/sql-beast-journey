# Day 2: Aggregations

- `COUNT(*)`, `SUM`, `AVG`, `MIN`, `MAX`
- `GROUP BY` groups rows; each column in SELECT must be in GROUP BY or be aggregate.
- `HAVING` filters groups; `WHERE` filters rows first.
- Real use: "Total revenue per country, only countries with >$1000."
